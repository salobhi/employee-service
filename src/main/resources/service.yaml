apiVersion: v1
kind: Service
metadata:
  name: tpg-demo-application-service
spec:
  ports:
    - protocol: "TCP"
      port: 8080        # The port inside the cluster
      targetPort: 8080  # The port exposed by the service
  type: NodePort        # Type of service
  selector:
    app: tpg-demo-application